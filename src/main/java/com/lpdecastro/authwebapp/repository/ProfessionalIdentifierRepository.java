package com.lpdecastro.authwebapp.repository;

import com.lpdecastro.authwebapp.entity.ProfessionalIdentifier;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProfessionalIdentifierRepository extends JpaRepository<ProfessionalIdentifier, Long> {
}
